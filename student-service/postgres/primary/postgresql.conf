listen_addresses = '*'
wal_level = replica
max_wal_senders = 3
max_replication_slots = 3
hot_standby = on
wal_keep_size = 64

# IMPORTANT: tell Postgres to use your pg_hba.conf
hba_file = '/var/lib/postgresql/data/pg_hba.conf'